{
  "name": "my-turborepo",
  "private": true,
  "scripts": {
    "build:backend": "cd apps/backend && yarn install && yarn build",
    "build:frontend": "cd apps/frontend && yarn install && yarn build",
    "build:docs": "cd apps/docs && yarn install && yarn build",
    "build:all": "yarn build:backend && yarn build:frontend && yarn build:docs"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.8",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "pg": "^8.16.3",
    "reflect-metadata": "^0.2.2",
    "typeorm": "^0.3.18"
  }
}
